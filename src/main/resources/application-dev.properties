# ================================================================
# Profile: dev
# Kích ho?t b?ng m?t trong hai cách:
#   1. IntelliJ > Run Config > Environment variables:
#      SPRING_PROFILES_ACTIVE=dev
#   2. Thêm vào .env:
#      SPRING_PROFILES_ACTIVE=dev   (ch? dùng Spring Boot >= 2.4, không dùng ???c trên 3+/4+)
#
# File này CH? ghi các giá tr? KHÁC v?i application.properties.
# Không l?p l?i DB URL, driver, hay các config ?ã có ? base.
# ================================================================

# ================= SEED =================
cms.seed.enabled=${CMS_SEED_ENABLED:true}
cms.seed.dense.enabled=false
cms.seed.dense.students=120
cms.seed.dense.teachers=12
cms.seed.dense.subjects=12
cms.seed.dense.courses=60
cms.seed.dense.enrollments-per-student=3
cms.debug.return-reset-token=true

# ================= FLYWAY =================
# validate=false ? dev ?? không b? l?i khi schema ?ang thay ??i liên t?c
spring.flyway.validate-on-migrate=false
logging.level.org.flywaydb=DEBUG